# Tomorrow First Actions

## First 3 actions

1. Confirm or restore the real `board-game-app` codebase path, then compare its package/build conventions against Worker B and Worker C before merging anything.
2. Review Worker B first, starting with `src/ingestion/contracts.ts` and `src/ingestion/validation.ts`.
3. Review Worker C second, starting with `src/ai/routing.ts`, `src/ai/policy.ts`, and `src/ai/config/default-model-profiles.ts`.

## Worker direction

- Worker A
  - Status: blocked or unavailable.
  - Action: needs scope correction or reactivation because no folder/report is visible.
- Worker B
  - Status: active, not done.
  - Action: continue with a narrower prompt focused on fixtures, tests, and confirmed-IR naming alignment.
- Worker C
  - Status: active, not done.
  - Action: continue with a narrower prompt focused on tests, one concrete provider adapter, and a small integration factory instead of more abstraction work.
